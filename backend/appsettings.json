{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
    "JWT": {
    "Issuer": "${JWT_ISSUER}",
    "Audience": "${JWT_AUDIENCE}",
    "SigningKey": "${JWT_SIGNING_KEY}"
  },
  "ConnectionStrings": {
    "DefaultConnection": "${DB_URL}"
  }
}
